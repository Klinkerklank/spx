// Returns 0 if a and b are equal
// Returns -1 otherwise
inline
fn __memcmp(reg ptr u8[SPX_N] a b) -> reg u8 {
    reg u8 t acc;

    inline int i;

    acc = 0;
  
    for i=0 to SPX_N {
        t = a[i];
        t ^= b[i];
        acc |= t;
    }

    return acc;
}